{
    "name": "algorithmia/legado",
    "description": "Algorithmia — RPG educacional em PHP puro. O jogo NÃO usa Composer em produção: nada aqui é carregado por index.php. Estas dependências existem só para os testes de caracterização que travam o comportamento do motor antes da migração para Laravel.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.3"
    },
    "require-dev": {
        "phpunit/phpunit": "^12.0"
    },
    "autoload-dev": {
        "psr-4": {
            "Algorithmia\\Testes\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit"
    }
}
